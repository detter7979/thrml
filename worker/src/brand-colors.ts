/** thrml brand rust — matches `fill="#C75B3A"` in block-split static SVGs. */
export const BRAND_RUST_HEX = "C75B3A"

/** FFmpeg filter color literal (no `#` prefix). */
export const BRAND_RUST_FFMPEG = `0x${BRAND_RUST_HEX}`
